
{
    "name": "javascript-template",
    "version": "1.0.0",
    "scripts": {
        "build": "node build.js",
        "dev": "node build.js --watch",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "esbuild": "^0.19.2",
        "terser": "^5.27.0",
        "rimraf": "^5.0.5"
    }
}
